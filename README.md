# quiz-telegram-bot

Bofore start start the bot, should:
1 Add .env file with the fields in the root project:
  GOOGLE_CREDENTIALS_PATH=./google-credentials.json
  BOT_TOKEN= ...
  SPREADSHEET_ID= ... //Google speadsheet id in google dive
2 Add google-credentials.json with the google credeantials for google access in the root project 